tasks:
  - init: |
      rm -rf .git
      rm ./README.MD
      python -m venv venv
      source venv/bin/activate
      pip install -U poetry

ports:
  - port: 8080
    onOpen: open-preview
    visibility: public

vscode:
  extensions:
    - ms-python.python
    - oderwat.indent-rainbow
    - usernamehw.errorlens
